# 스마트 창고 출고 지연 예측 — Python 패키지 의존성
# Python 3.11 권장
# 설치: pip install -r requirements.txt

# 수치/데이터
numpy>=1.26,<2.1
pandas>=2.1,<2.3
scipy>=1.11
pyarrow>=15.0

# 머신러닝 (트리 부스팅 + 베이스)
scikit-learn>=1.4,<1.6
lightgbm>=4.3
xgboost>=2.0
catboost>=1.2.5

# 하이퍼파라미터 튜닝 (후순위, 라인업 확정 후 사용)
optuna>=3.6

# 시각화
matplotlib>=3.8
seaborn>=0.13

# 유틸
tqdm>=4.66
joblib>=1.3

# 노트북 (선택, EDA·실험용)
jupyterlab>=4.1
ipykernel>=6.29
ipywidgets>=8.1
