# 모기 비행 궤적 예측 — Python 패키지
# Python 3.11 권장. PyTorch 는 SETUP.md 3장 참고하여 별도 설치.

# ─── 수치/데이터 ───
numpy>=1.26,<2.1
pandas>=2.1,<2.3
scipy>=1.11
pyarrow>=15.0       # parquet 캐싱

# ─── 머신러닝 (트리 부스팅) ───
scikit-learn>=1.4,<1.6
lightgbm>=4.3
xgboost>=2.0
catboost>=1.2.5

# ─── 시계열 특화 ───
statsmodels>=0.14   # ARIMA 등 고전 시계열
filterpy>=1.4       # Kalman filter
# pykalman 도 가능한 대안

# ─── 딥러닝 보조 (PyTorch 는 SETUP.md 참고) ───
pytorch-lightning>=2.2   # 학습 루프 편의
einops>=0.7              # tensor reshape 편의
transformers>=4.40       # HuggingFace foundation models
huggingface-hub>=0.22    # 사전학습 모델 다운로드
accelerate>=0.30         # 멀티 GPU / 혼합정밀도

# ─── 하이퍼파라미터 / 실험 추적 ───
optuna>=3.6
# wandb>=0.16            # 선택 — 실험 추적

# ─── 시각화 ───
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20             # 3D trajectory 시각화

# ─── 유틸 ───
tqdm>=4.66
joblib>=1.3

# ─── 노트북 (선택) ───
jupyterlab>=4.1
ipykernel>=6.29
ipywidgets>=8.1
